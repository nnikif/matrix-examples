import { SomeDirDirective } from './some-dir.directive';

describe('SomeDirDirective', () => {
  it('should create an instance', () => {
    const directive = new SomeDirDirective();
    expect(directive).toBeTruthy();
  });
});
